{
  "name": "@opencomments/backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@opencomments/types": "workspace:*",
    "@types/pg": "^8.15.6",
    "better-auth": "^1.3.34",
    "bun-plugin-tailwind": "^0.1.2",
    "hono": "^4.10.4",
    "pg": "^8.16.3",
    "tailwind": "^4.0.0"
  },
  "scripts": {
    "dev": "bun --watch index.ts",
    "build": "bun install --frozen-lockfile || bun install",
    "deploy": "bun install --frozen-lockfile || bun install && wrangler deploy"
  }
}
